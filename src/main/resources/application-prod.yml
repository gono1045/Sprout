spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
    driver-class-name: org.postgresql.Driver

    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        jdbc:
          time_zone: UTC

  mail:
    host: ${MAIL_HOST}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

  # 静的リソースのブラウザキャッシュ期間（同一ファイルの再取得を減らし表示を高速化）
  web:
    resources:
      cache:
        cachecontrol:
          max-age: 7d

app:
  base-url: ${APP_BASE_URL:https://sprout-i5mi.onrender.com}
  mail:
    from: ${MAIL_FROM:${MAIL_USERNAME}}

mybatis:
  mapper-locations: classpath*:mappers/**/*.xml
  type-aliases-package: com.example.sprout.model
  configuration:
    map-underscore-to-camel-case: true

server:
  port: ${PORT:8080}

logging:
  level:
    org.mybatis: WARN
    org.mybatis.spring: WARN
